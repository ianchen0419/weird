/**
 * Expressions
 * @description
 * 
 * ```js
 * var a = 3;
 * ```
 * 
 * `=`是一個Operator（簡化成符號的Function），他可以將左邊與右邊的值做一些處理，最後**回傳一個值**。
 * 因為`var a = 3;`有回傳，所以他是一個Expression
 */


 var a;

 a = 3; // Valid Expression
 1 + 2; // Valid Expression
 a = { greeting: 'hi' }; // Valid Expression