/**
 * Statements
 * @description
 * 
 * `if`是Statement，因為沒有回傳值。
 * 
 * ```js
 * var b = if(a === 3) { } // No work
 * ```
 * 上面這段程式不合法，因為`if`沒有回傳值。
 * 
 * `if`後面的括弧`()`裡面要放Expression，並且這個Expression的回傳值需要是`true`或是`false`。
 * 
 */

var a;

if (a === 3) {

}

